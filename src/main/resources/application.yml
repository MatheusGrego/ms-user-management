spring:
  jackson:
    serialization:
      fail-on-empty-beans: false
  application:
    name: IRenova-check
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/IRenova-check?currentSchema=IRenova-check
    username: postgres
    password: toor
  jpa:
    properties:
      hibernate:
        default_schema: IRenova-check
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
  rsocket:
    server:
      address: 26.140.117.70
